import styled from 'styled-components';

const DateTime = styled.span({
  position: 'absolute',
  bottom: '10px',
  right: '10px',
  fontSize: '.6rem',
  color: 'var(--fontColor)',
});

export default DateTime;
