import styled from 'styled-components';

const Brand = styled.h1({
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  letterSpacing: '1px',
  fontSize: '2rem',
});

export default Brand;
